<div class="row-fluid">
	<div class="span10 offset1">
		<p id="notice"><%= notice %></p>
		<br />
		<br />
		<table class="table table-striped table-hover table-condensed">
			<tr>
		  	<td class="thead">Title:</td>
		  	<td class="thead">Descripton:</td>
		  	<td class="thead">User:</td>
		  	<td class="thead">Date Create:</td>
		  </tr>
		  <tr>
			  <td><%= @bdcard.title %></td>
			
			  <td><%= @bdcard.description %></td>
			
			  <td><%= @bdcard.user_id %></td>
		 
			  <td><%= @bdcard.date_created %></td>
			</tr>
		</table>
		<%= @match %>
		<br />
		<h4>Post this Qube to your friend's facebook wall.</h4>
		<form action="/fbwall" method="get">
			<input type="hidden" name="link" value="/bdcards/<%= @bdcard.id %>">
			<input type="hidden" name='fbid' value="<%= @match[0] %>"></a>
			<button class="btn">Post to Wall</button>
		</form>
		<%= link_to 'Edit', edit_bdcard_path(@bdcard) %> |
		<%= link_to 'Back', bdcards_path %>
	</div>
</div>
